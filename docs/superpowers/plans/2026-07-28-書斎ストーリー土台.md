# 書斎ストーリー 土台（第1弾）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 物語レイヤーの「Rive非依存・実費ゼロ・後で捨てない土台」を実装する（毎回起動の1日1回ゲート＋お守り撤去）。

**Architecture:** 状態に `lastGreetDay` を1つ足し、出迎え判定を純関数に切り出す（UI配線は後続プラン）。ショップからお守りを撤去する。既存ロジック（報酬・購入・装備）には触れない。

**Tech Stack:** TypeScript / React Native (Expo) / 既存テストランナー（jest 互換・`npm test`）。

## Global Constraints

- 課金・紹介・上限・出題・ショップ購入/装備の**ロジックは変えない**（本弾は状態1フィールド追加とデータ1件削除のみ）。仕様: `docs/superpowers/specs/2026-07-28-書斎ストーリー-design.md`。
- 純関数は副作用なし・入力不変（既存 [wallet.ts](../../../src/store/wallet.ts) の作法に合わせる）。
- 日付は `dayStr(now)`（[state.ts:137](../../../src/store/state.ts#L137)）を使う。`Date.now()` は呼び出し側から渡す（テスト可能に）。
- Rive・新規AI画像・起動演出・タブ改名・貝殻リング・苦手自動検知は**本弾に含めない**（末尾ロードマップの別プラン。Rive/画像は着手前に見積り＝D1）。

---

### Task 1: 毎回起動の「1日1回」出迎えゲート（純関数）

**Files:**
- Modify: `src/store/state.ts`（`AppState` に `lastGreetDay?: string;` を追加。77-78行の `claimedMilestones?` / `dailyEarn?` の並びに置く）
- Create: `src/home/greeting.ts`
- Test: `src/home/greeting.test.ts`

**Interfaces:**
- Consumes: `AppState`・`dayStr(ts: number): string`・`INITIAL_STATE`（すべて `../store/state`）
- Produces:
  - `shouldGreetToday(state: AppState, now: number): boolean`
  - `markGreetedToday(state: AppState, now: number): AppState`

- [ ] **Step 1: 失敗するテストを書く**

`src/home/greeting.test.ts`:
```ts
import { shouldGreetToday, markGreetedToday } from './greeting';
import { INITIAL_STATE } from '../store/state';

const T = Date.UTC(2026, 6, 28, 1, 0, 0); // 2026-07-28 相当
const DAY = 24 * 3600 * 1000;

test('初回(lastGreetDay 無し)は出迎える', () => {
  expect(shouldGreetToday(INITIAL_STATE, T)).toBe(true);
});

test('同じ日の2回目は出迎えない', () => {
  const s = markGreetedToday(INITIAL_STATE, T);
  expect(shouldGreetToday(s, T)).toBe(false);
});

test('日付が変わったら再び出迎える', () => {
  const s = markGreetedToday(INITIAL_STATE, T);
  expect(shouldGreetToday(s, T + DAY)).toBe(true);
});

test('markGreetedToday は入力を変更しない(純粋)', () => {
  const snapshot = JSON.stringify(INITIAL_STATE);
  markGreetedToday(INITIAL_STATE, T);
  expect(JSON.stringify(INITIAL_STATE)).toBe(snapshot);
});
```

- [ ] **Step 2: テストが落ちることを確認**

Run: `npm test -- src/home/greeting.test.ts`
Expected: FAIL（`greeting` モジュールが存在しない）

- [ ] **Step 3: 状態に1フィールド追加**

`src/store/state.ts` の `AppState` 内、`dailyEarn?: { day: string; amount: number };` の次の行に追加:
```ts
  lastGreetDay?: string;                 // 毎回起動の「今日の出迎え」を1日1回に絞る(dayStr)
```

- [ ] **Step 4: 純関数を実装**

`src/home/greeting.ts`:
```ts
// 毎回起動の「今日の出迎え」を1日1回だけ出すための純関数。UI配線は後続プランで行う。
import { dayStr, type AppState } from '../store/state';

/** 今日まだ出迎えていなければ true（lastGreetDay が今日と違う）。 */
export function shouldGreetToday(state: AppState, now: number): boolean {
  return state.lastGreetDay !== dayStr(now);
}

/** 出迎え済みとして当日を記録した新しい状態を返す（純粋・入力は不変）。 */
export function markGreetedToday(state: AppState, now: number): AppState {
  return { ...state, lastGreetDay: dayStr(now) };
}
```

- [ ] **Step 5: テストが通ることを確認**

Run: `npm test -- src/home/greeting.test.ts`
Expected: PASS（4件）

- [ ] **Step 6: 型チェック**

Run: `npx tsc --noEmit`
Expected: エラー0

- [ ] **Step 7: コミット**

```bash
git add src/store/state.ts src/home/greeting.ts src/home/greeting.test.ts
git commit -m "feat(story): 毎回起動の1日1回 出迎えゲート(純関数)＋lastGreetDay"
```

---

### Task 2: ショップからお守りを撤去

**Files:**
- Modify: `src/data/shop.ts`（`tool_omamori` の1行を削除。59行目付近）

**Interfaces:**
- Consumes: なし（データ定義の削除のみ）
- Produces: なし

**背景（安全性）:** 所持アイテム表示 [InventoryScreen.tsx:41](../../../src/screens/InventoryScreen.tsx#L41) は `(state.owned ?? []).map((id) => SHOP_BY_ID[id]).filter(Boolean)` で未知idを除外済み。よって既にお守りを購入済みのユーザーの状態でも**クラッシュしない**（一覧から静かに消えるだけ）。他の参照は `tool_omamori` について無し（brush/costume/companion のみ SHOP_BY_ID を引く）。

- [ ] **Step 1: 参照が他に無いことを確認**

Run: `git grep -n "tool_omamori"`
Expected: `src/data/shop.ts` の定義行のみ（他ファイルに参照なし）

- [ ] **Step 2: お守りの行を削除**

`src/data/shop.ts` の「道具」セクションから次の行を削除:
```ts
  { id: 'tool_omamori', cat: 'tool', kind: 'tool', price: 400, name: '合格お守り', emoji: '🧧' },
```
（残る道具＝模試チケット・再挑戦券・連続フリーズ の3つ）

- [ ] **Step 3: 型チェック**

Run: `npx tsc --noEmit`
Expected: エラー0

- [ ] **Step 4: 既存テストが緑のまま**

Run: `npm test`
Expected: PASS（お守り撤去で壊れるテストが無いこと。もし `tool_omamori` を参照するテストがあれば、そのテストを削除・修正してから再実行）

- [ ] **Step 5: コミット**

```bash
git add src/data/shop.ts
git commit -m "feat(story): ショップからお守り(tool_omamori)を撤去"
```

---

## 後続プラン（本弾の外・順に別プランで作成）

各プランは単体で動作・テスト可能にする。Rive/新規AI画像を含むプランは**着手前に点数×単価を見積もり提示**（D1）。

1. **タブ改名＋i18n**：単語→書斎／辞書→書庫（10言語。study.tab は現状「試験」を維持 or 腕試しへ・要判断）。
2. **貝殻リング**：`PassRing` を貝殻モチーフへ衣替え（既存🐚流用・ロジック不変）。
3. **苦手自動検知**：適応ラダーから苦手語を拾い手習い帳へ（判定基準を先に確定）。手動登録は現状維持。
4. **毎回起動の出迎えUI**：Task1のゲートに配線（桜＋犬の出迎え・今日のひとこと）。
5. **学習後の演出（P1）**：[SessionSummary.tsx](../../../src/components/SessionSummary.tsx) 表示時に貝殻＋一筆＋犬。**Rive**（要見積り・要再ビルド・runtimeVersion↑）。
6. **初回入場物語**：[OnboardingScreen.tsx](../../../src/screens/OnboardingScreen.tsx) の前に2〜3枚（AI画像・要見積り）。
7. **機能の衣**：課金＝奥書斎／紹介＝灯を分ける／模試＝大試（既存ロジックに文言・演出）。

---

## Self-Review

- **Spec coverage（本弾担当分）**：お守り撤去（§3 表・§6-4）＝Task2 ✅／毎回起動1日1回（§6-2 の判定土台）＝Task1 ✅。その他の §は後続プランに割当（上記ロードマップ）で明示。
- **Placeholder scan**：TBD/TODO 無し。全ステップに実コード・実コマンド・期待結果あり。
- **Type consistency**：`shouldGreetToday`/`markGreetedToday` の型は Task1 内で一貫。`lastGreetDay?: string` と `dayStr(): string` 整合。
