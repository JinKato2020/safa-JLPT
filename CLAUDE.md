このフォルダ = 「まいにちJLPT」アプリ専用セッション

JLPT合否「到達度」を測り、毎日の学習で上げていく。企画〜実装〜公開まで本セッションが担う。

- 全セッション共通の規律（トークン・課金・ビルド・伝え方）＝ 親 `..\CLAUDE.md`

## 1. 問題作成

- 大問ごとの作問データ＝ `md\<大問名>.md`（大問名と同じファイル名）／全大問共通＝ `md\共通情報.md`
- 手順＝ `.claude\skills-archive\daimon-question-build\SKILL.md`（試験タブの大問）/ `.claude\skills-archive\listening-skill\SKILL.md`（聴解音声）を必要時に Read（**索引型・毎回はロードしない**。索引＝`.claude\skills-archive\README.md`）
- 在庫数＝ `memory\在庫問題数.txt`（`tools\stock_report.py`。Stop hook が**問題ファイル変更時だけ**自動更新）
  - 在庫＝**公式問題にそって新しく作成した問題**のみ。辞書から機械生成した漢字読み・表記、監査に落ちた分（hold / ambiguous / verified=false）は数えない
- **【動詞の丁寧形・時制の扱い・永久】** 時制/丁寧形（ます/ました/ません/ましょう）は公式にも出るが、**価値は「本文依存か」で決まる**：
  - **本文依存（文章の流れ・時制の一貫性・視点で決まる）＝正当な出題**。残す・作ってよい（例：文章全体が過去→「入りました」）。集約点 `n5-g-87`「動詞の丁寧形・時制」。
  - **一文で解ける活用ドリル（本文を読まず局所で決まる）＝薄い**。**今後は新規作成しない**。既存分は指標対象外点 `n5-g-92`「動詞の活用ドリル（本文非依存）」へ隔離（passage 33問）。**出題はするが学習指標には数えない**（下記 METRIC_EXCLUDED）。
  - 番人＝`src\data\grammarExclude.test.ts`（バラ活用の点が n5-g-87 以外に増えると失敗）／`src\data\metricExclude.test.ts`（対象外点が母数から除外されているか）。
- **【模試プールの語 重複禁止・永久／仕組み実装】** 模試は各大問の専用プール（`content/problems/.../mock/`・pool='mock'）から出題。**同一模試内では同じ語彙/漢字/文法の語を2つ以上の大問に出さない**（大問横断で語をユニーク化）。**プール自体は大問間で語が重なってよい**（模試は10回分あり、別回なら再利用可）。実装＝模試組み立て（`MockScreen` `knowledgeForDaimon`）で `usedWords` を大問横断で共有し既使用語をスキップ。詳細＝メモリ `[[mock-cross-daimon-no-word-reuse]]`。
- **【一意性 自己申告・永久／全模試フロー共通】** 一意性の最終判断はユーザー（私は意味検証エージェントを走らせない）。ただし**生成エージェントは作問と同時に各itemへ自己申告**を付ける＝`uniqRisk:"high"|"mid"`（一意に自信あれば欄なし）＋付けたら `uniqNote` に理由1文。書出し＝`python tools\quality_excel.py`→`一意性チェック_{模試,通常}_{N5,N4,N3}.xlsx`（レベル毎ファイル・大問毎シート・🔴high/🟡mid色付け）でユーザーがレビュー。仕様＝`md\一意性自己申告ルール.md`／メモリ `[[uniqueness-self-declaration-in-generation]]`。

## 2. 共通仕様

- **ルビ**＝自分のレベルと同じ漢字か、自分のレベルより上の漢字に付ける（聴解のスクリプト表示も同じ）
- **辞書タブ／単語タブの漢字リスト**＝自分のレベル以下の読みだけを例題表示。自分のレベルより上の読みは詳細を開いた時だけ表示
- **【UI多言語化＝新規UIは必ず言語フィールド付き・英語+ネパール語を同時作成（ユーザー厳命 2026-08-14・厳守）】** 母語は10言語から選べ（`NATIVE_LANGS`）、`en`(英語)/`ne`(ネパール語)は表示言語として常時提供。**UI文字列を追加する時は `ja.json` だけでなく `en.json`・`ne.json` にも同時に訳を入れる**（jaだけだと en/ne 選択時に日本語が出るバグ＝相対位置カードで実害）。他8言語(bn/id/ko/my/th/vi/zh)は従来どおりバックログ運用（`tools\i18n_backlog.py`・指示時のみ一括翻訳）。**仕組み＝番人テスト `src\i18n\parity.test.ts`**（ja の全キーが en/ne に有るか＋プレースホルダ`{n}`等の一致を検査・欠落でビルド失敗）。※旧メモリ「新規UIはja.jsonだけ・先回り翻訳禁止」は en/ne について失効（他8言語には有効）。

## 3. /clear をまたぐ状態（A3の実装）

- 状態の正本＝ `memory\handoff.md`。SessionStart hook が毎回これを読み込むので、**会話にしか無い情報を作らない**
- 「## 次の一手」の1行だけ私が手で更新（区切り・方針決定の時に、報告の前に）。走行中 run と直近変更ファイルは Stop hook が自動で書く
- 過去の会話を掘る＝ `python tools\find_history.py "語1" "語2"`（複数語＝AND。`--since 日付` `--role user` で絞る。自分のログ・ツール入力・自動挿入文は既定で除外。上限超過時は件数＋日付＋直近3件だけ。全文は絶対に会話に載らない）

## 4. 予想得点・カバー率の算定（習得の正本＝面別マスタリー `state.mastery`）

コード＝`src\store\selectors.ts`。習得は**単語/文法点 × 面(read/write/mean/listen/grammar)**で追跡（面別マスタリー）。文法は穴埋め/組み立て/文章の文法が**すべて pointId の grammar 面**に合流。

- **予想得点**（`expectedScoreFor`→`ladderPassEntries`）＝①各大問の**予測正答率 μ**＝その大問の全問の習得度→正答確率に変換し平均（**未着手=0.25**で薄める＝カバー率が効く）。②各大問を**本番の出題数 n**で重み付け。③(n,μ) を公式の得点区分（言語知識/読解/聴解）に配点し **180点満点**の期待値（大リング合格率と同配点）。
- **カバー率**（`coverageBars`）＝**分母＝レベル内の文法点数**、**分子＝grammar 面≥0.6 の点数**。「何点の文法を覚えたか」の広さ指標（問題数ではない）。
- **重要**：どちらも **pointId 経由**で習得を読む。だから **pointId の貼り直し（正解＝pointID）は予想得点・カバー率を正確化する**（誤紐づけ=別の点に習得が入る）。配信・正誤判定は問題ID基準で pointId 不使用。
- **文法のnovelty重み**＝少ない出題形式ほど1回の正誤を強く反映（λ=1・最大2倍・全形式強制でない）。詳細＝メモリ `grammar-format-novelty-mastery`。
- **指標対象外の文法点**＝`src\data\exam\metricExcludedPoints.json`（現状 `n5-g-92`＝本文非依存の活用ドリル）。**出題・採点はするが、カバー率・予想得点・リングの母数から除外**（`daimonUnitIds` の passage フィルタ＋`coverageBars` のスキップ）。薄い水増し問題を学習指標に数えないための仕組み。

## 参照（毎回は読まない）

- 本セッションのトークン浪費 実害履歴（16問に5万トークンの誤見積り／往復だけで1日9,000万トークン等）＝ `..\md\事故例_トークン浪費.md`
