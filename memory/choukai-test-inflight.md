# 課題理解 100問化 テスト先行（inflight）

## 目的
本番（各レベル0044-0100の大量作問）の前に、新設計（非手順の型＝物/場所/数時/放送・N3間接・素→モーラ→ルビ・係→スタッフ）で**各レベル3問（0041-0043）＝計9問**を作り、**音声生成まで**通しでテストする。良ければ本番へ展開。

## 走行中 run
- `br5c3ojep`＝9問の音声生成（`gen_choukai_json.py`・Gemini2.5Flash TTS）。出力＝`assets/audio/{id}.mp3`＋`問題/聴解/{lv}/kadai/{id}.mp3`＋パイロット複製 `問題/聴解/パイロット_課題テスト0041/`。ログ＝`memory/choukai_gen_log.txt`（末尾に実費・円）。完了通知で1回だけ回収（ポーリング禁止＝A4）。

## 済んだこと
- 9問を素で作問→モーラ帯ゲート（致命0/帯外0/係0）→フルルビ付与→to_ttsプレビュー確認（FORCE_KANA正＝十日/二十四日/一日=いちにち）→正本 kadai_{N5,N4,N3}.json へ追記（各40→43問）。
- 型＝全問非手順（N5:物/場所/数時、N4:数時/物/放送、N3:物(消去法)/放送/数時いつ）。放送(独話)2問=N4-0043,N3-0042。
- 仕組み改修：merge_and_gate の SPK/LABEL に役割注記「女1（客）：」を剥がす `(?:（[^）]*）)?` を追加（音声の turns_of と一致・既存正本の役割ラベルもゲート可に）。

## テスト音声（0041-0043）＝完了・合格判断済み
- 9本生成成功・$0.19≈29円。ユーザー試聴OK→本番着手の合図（「これで44-60問を作成して」）。
- LCS: 8問≥0.80／N3-0043のみ0.74（日付周り・要注視だが本番続行）。

## 本番バッチ 0044-0060（各17問=51問）＝正本追記済み・音声生成中
- 3体生成→検証で見つけた不具合を全修正して正本へ追記済み（各レベル43→60問）：
  - 全レベル：導入直後の空行欠落→`fix_blank.py`（恒久ツール化）／N5：導入行ごと欠落→N5エージェントに差し戻して導入付与。
  - merge_and_gate 係チェックの誤検出（関係＝かんけい）→正規表現を精緻化（関/連前・数/長/わり後を除外）。
  - N3-C-K-0052「三日後（みっかご）」ユニットルビでforce-kana不発→「三日（みっか）で」に言い換え。フロー ルビ節に注意追記。
- 最終ゲート：手順0（全レベル）・mora全帯内・致命0・帯外0・係0・N3ネタバレ0・熟字訓日付の生ゼロ。
- ✅**完了**：51問の音声生成 `bbysxip50`＝51/51成功・失敗0・**実費 $1.08=約162円**。pilot=`問題/聴解/パイロット_課題テスト0044/`。低LCSの洗い出しは未実施（必要時）。

## 次のバッチ＝0061-0080（各20問=計60）★走行中（2026-08-13起動）
- **作問サブエージェント3体（general-purpose・素→自己mora_check→ルビ完結・各レベル20問）走行中**：
  - N5 → agentId a319d8af039daa695
  - N4 → agentId a61b52ddaf266d2b4
  - N3 → agentId ac66bfc51560cbbb8
- 出力＝`scratchpad_choukai/newq_0061/new_kadai_{N5,N4,N3}.json`（各20件・ルビ付き）。完了通知で回収（ポーリング禁止＝A4）。
- **3体完了→ゲート通過（2026-08-13）**：fix_blank=修正0（空行/導入OK）。`new_kadai.json`統合(60件)。merge_and_gate=**致命0/帯外0**（mora N5 106/126/146・N4 219/238/280・N3 269/314/357 全帯内・係残0・choices/id/重複OK）。qtype_ledger --only=**全レベル手順0**・型分布OK・**N3直接ネタバレ0**。場面重複は参考のみ（薄カテゴリ集中の設計どおり・非ブロック）。
- **N3ルビ修正完了**（agent ac66bfc51560cbbb8）：全漢字ルビ化(総1210・avg60.5≒既存59)・strip_furi一致・未ルビ0。バンク整合。
- **--apply完了**：正本 N5/N4/N3 各60→**80**（+60・除外0）。末尾=各`-0080`。
- **tts_lint**：初回2件(N4-0069『一日中使（…）』・N4-0070『大人一人（…）』＝FORCE_KANA語を大きなルビ単位が飲み込み)→ルビ分割で修正(一日（いちにち）中（じゅう）/大人（おとな）一人（ひとり）・strip_furi不変)→**再lint 0/60✅**。正本+scratchpad両方修正済。
- **音声生成 走行中**：run `bdyqy6ai5`（`gen_choukai_json.py --ids-file _ids.txt(60) --pilot 問題/聴解/パイロット_課題0061`）。ログ=`memory/choukai_gen_log_0061.txt`。完了通知で実費回収（A4ポーリング禁止）。前バッチ51問=¥162なので60問≈¥190見込。
- **音声後の最終仕上げ（未・区切りで一括）**：`tools/content/rebuild.ts`で`_manifest.json`+`bundled.generated.ts`再生成→`問題/tools/tts_script.py`で音声台帳更新→コミット。これをやるまでアプリに新問(0041-0080)は出ない。
- **別トラックUI（未コミット・未ビルド）**：町ステータス枠=覚えた単語をバー下に『覚えた/総単語(レベル総数)』分数化＋ラベル/値を枠内上下中央揃え／ホーム桜の吹き出しを立ち絵の頭上に配置(iPad顔かぶり対策・全機種でリングと桜の間)。KotobaTownScreen.tsx/SakuraSpeech.tsx。typecheck緑。ビルド指示待ち。
- 設計（起動時に指示済）：**手順ゼロ**／物・場所・数時・放送を混ぜる（欠けた型を埋める）／N3は間接（消去法・本音後半・『まずは〜』ネタバレ禁止）／係→スタッフ／導入行＋空行＋男女始まりラベル／熟字訓日付+接尾語ユニットルビ回避。
  - 設問型の現状(正本60/60/60)＝全レベル手順40＋残20。薄い型：N5=何を/放送、N4=数時/何を、N3=数時/何を。
  - 場面の薄い所：N5=会社/病院/公共、N4=地域近所/病院/公共、N3=店買い物/公共/地域。
- **3体そろったら**：`fix_blank.py`（空行/導入点検）→`merge_and_gate.py --new scratchpad_choukai/newq_0061`（帯・係・重複確認）→`qtype_ledger.py --only scratchpad_choukai/newq_0061`（手順0・型分布・N3ネタバレ）→帯外/型偏り/係残はSendMessageで該当agentに直させ（ルビ保持）→全OKで`--apply`（正本60→80）→**`tts_lint.py --new …`（FORCE_KANA生残り点検）**→音声`問題/tools/gen_choukai_json.py --ids <60id> --pilot …`。
- 既出テンプレ回避（0001-0060で使用済）＝病院初診/銀行口座/図書館貸出/遠足持ち物/研修受付/プレゼント消去法/バス集合/かばん/花/たまご数/会議室荷物。
- **⚠未コミット（別トラック）**：正本kadai 0041-0060・assets/audio mp3(0041-0060)・tools・md。bundled.generated/_manifest未再生成＝アプリ未反映。UI修正(ステータス枠)はbuild2788で提出済(コミットf4c0d847)。

## （旧）テストバッチ走行 run
- 3レベル並列サブエージェント（general-purpose）。各自 素→mora_check→修正→フルルビ を完結し書き出す：
  - N5 → agentId ab5b13c7370020e1b
  - N4 → agentId aef561d437f931bb9
  - N3 → agentId a8d7a48ae36df77b2
- 出力＝`scratchpad/newq_0044/new_kadai_{N5,N4,N3}.json`。完了通知で回収（ポーリング禁止）。

## 次の一手（3体そろったら）
1. 統合 new_kadai.json 生成 → `merge_and_gate.py --new`（致命/帯外/係を確認）＋`qtype_ledger.py --only`（手順0・型分布・N3ネタバレ）＋ to_tts プレビュー（FORCE_KANA読み）。
2. 帯外/型偏り/係残存があれば該当エージェントへ SendMessage で直させる（同一文脈でルビ保持）。
3. 全OKで `--apply` → 正本 0044-0060 追記（各43→60問）。
4. 音声＝`gen_choukai_json.py --ids <51id> --pilot 問題/聴解/パイロット_課題テスト0041`（or 新pilot）。試聴。
5. content編集後 → tts_script台帳更新 → `_manifest.json` 再生成（OTA）→ ビルドは指示時のみ。
6. 残り 0061-0100（各40問）は本バッチ品質OK後に同フローで。

## 破棄予定
- 旧スクラッチの 0041-0100 素案（newq20/newq_0061/newq_0081）＝全部手順で不採用。新設計で作り直す。
