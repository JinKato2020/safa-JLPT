# handoff（/clear 耐性・上書き式・常に最新のみ）

## 次の一手
- **💰 マネタイズ設計＋Phase0実装計画 確定（2026-07-26・commit予定）**: 設計書=`docs/superpowers/specs6-07-26-monetization-design.md` / 実装計画=`docs/superpowers/plans6-07-26-monetize-phase0-entitlement.md`（Task1-6・TDD）。**確定事項**: 無料=1日3回(約20〜30問)・広告1本で+1回/1日2本まで→最大5回。**レベル制限(N4/N3ロック)は不採用**(28ファイル波及＋体験悪)。「今日のおすすめ」はPro限定にせず無料(ただし1回消費)。広告報酬は練習+1回のみ(模試チケット無し)。お試し=7日(戦略板14日から変更)。Android=`com.safa.english`(App C枠)のまま＝**コード変更ゼロ**。iOS=`com.safa.jlpt`。RevenueCat(課金)＋AdMob(広告)。**Phase0=`GATING_ENABLED=false`で出荷＝誰も制限されない**(仕組みだけ完成)。Phase1で購入画面と同時にtrue。**次の一手**: Phase0の計画をTask1から実装（`src/pro/entitlement.ts`→`dailyQuota.ts`→store配線→7画面）。**Phase1/2はユーザーの手作業待ち**=RevenueCatアカウント＋ストア課金商品作成／AdMobアカウント＋広告ユニット＋app-ads.txt。**戦略板へ報告すべき差分5件**（お試し7日／AndroidはApp C枠／レベル制限→1日3回／おすすめは無料／広告報酬は+1回のみ）は未報告。
- **✅ Build 2553/both 投入・全260聴解実装確定（2026-07-26・commit 2aa53c5・push済・run 30177746932）**: N3音声を公式話速で再生成(100本)＝**聴解260問=データ260＋音声260本すべて実在・bundled.generated.tsに焼込確認・_manifest再生成**。同梱=Google/Apple認証(Web OAuth)・仮名書き取り(行選択ア〜ワ/見ないでランダム/漢字画面の速度・ドリルボタン削除)・管理ダッシュボード統合(登録匿名1表・大問別/単語帳数/カバー率3分割・snapshot v3)・単語タブ完了画面を試験タブに統一・プライバシー10言語。**dispatch=platforms=both/build_number=2553/submit=true(iOS→TestFlight)/aab=false・publish=false(Androidはビルドのみ・Play非公開)**。push契機でPages OTAも起動(新音声260＋content配信)。tsc=触ったファイル0エラー(既存11=submodule/Deno無関係)。**次: Actions run緑を確認しE2報告(iOS TestFlight＋Android＋OTA)。監視ポーリングはしない(通知/ユーザー確認待ち)。** D2実費: N3音声≈243円(Gemini 2.5 Flash TTS)。
- **✅(完了・上記Buildで出荷) 聴解 音声バッチ＝レベル分割（2026-07-25・ユーザー承認「N5→確認→N4→N3」）**: N5の76問（各大問K/P/H/S × 002-020）を生成 **完了=66/76・$0.53≒79円**（`bw1ne1af4` exit0・2026-07-25）。Excel反映済=75/80。1回目の失敗10件を再生成→5回復(P群)。**残5件(K-006/007/015/019・P-010)は決定的400=「Model tried to generate text...only be used for TTS」**＝長い品質プロンプト＋末尾疑問文でFlashが読み上げでなく“回答”しテキスト生成→AUDIO専用で弾かれる確率的事故。**真因=生成器がコスト集計で`M.flash_pcm`を`flash_pcm_acc`(gen_choukai_json)に差し替えており、build_choukai3側のガードは素通り**。**修正=`flash_pcm_acc`にガード投入済**(text生成400を踏むと以降`M.SAFE_READ`前置でループ内再試行→尽きれば`RuntimeError`化→`one()`のChirp3フォールバック)。同ガードは`build_choukai3.flash_pcm`にも(標準経路用)。**✅5件を再生成→5/5成功（修正版・約9円）＝N5 80/80完了・Excel反映済80/80・N5実費≈99円**。**✅N5ユーザー承認済→N4(76問=K/P/H/S×002-020)生成中=bg `bq7w40250`**（`--fresh`・改善版Gemini・読み上げ専用ガード入り・IDリスト=scratchpad `n4_ids.txt`）。**完了後**: `update_choukai_xlsx.py N4`→ユーザー確認→**N3(95・概要G含む)**。※発話HはN5同様Chirp3-HD→Gemini Ledaへ更新（ユーザーに事前告知済・異議あれば発話除外）。同ガードでN4/N3も同種400に強い。残見積≈530円（同期API・バッチ半額はTTS非対応）。生成記録=`memory\choukai_audio_rec.json`（生成器がid→尺・声を追記）。詳細=`md\聴解_音声作成フロー.md`冒頭。
- **🔊 聴解 音声＝Gemini 2.5 Flash TTS フロー 本採用・確定（2026-07-25。旧OpenAI/VOICEVOX二分化は無効）**: エンジン=`gemini-2.5-flash-preview-tts`。ナレーター(導入・設問)=**Aoede**／対話=男1 Orus・男2 Fenrir・女1 Leda・女2 Kore／**独話系(発話・即時・概要)=既定 Leda・台本が男性役(父/息子/男教師/兄弟/夫等)のときだけ Orus**（`gen_choukai_json.py` の `hs_voice`＝MALE_RE/FEMALE_RE。旧「奇数Orus/偶数Leda」は廃止）。番号1〜4・発話末尾「なんと言いますか」= `_numbers\{Leda,Orus}\*.wav` の合格クリップ**固定読込(TTS生成しない)**。**プロンプト品質3本柱=①速度ゆっくり/早口にしない ②文末(語尾)まで声を保ち言い切る ③東京式ピッチ正確/漢字読み間違えない**（`build_choukai3.py` NARR/DIA/BLOCK_STYLE＋`gen_choukai_json.py` CHOICE_STYLE）。生成=`python 問題/tools/gen_choukai_json.py --ids <csv>|--ids-file <f> [--fresh]`→`問題\聴解\{lv}\{大問}\{id}.mp3`＋`assets\audio\{id}.mp3`・ログ`memory\choukai_gen_log.txt`(末尾に実費円)。旧音声退避=`問題\聴解\パラスタ\旧音声バックアップ_2026-07-25\`(117本)。Excel3ファイルに「音声プロンプト指示」列追加済。**✅パイロット13本(各大問001)再生成完了=13/13・$0.18≒28円・H/S全Leda・改善版で尺+12〜25%(ゆっくり化)・N5-P LCS0.66はユーザー試聴OK**。次=試聴確認→残り問題へ展開→**OTA `_manifest.json`再生成**必須(旧指紋だと新音声が届かない[[ota-manifest-regen-or-stale]])。速度不足なら保険=ffmpeg atempo(ピッチ維持)。手順正本=`md\聴解_音声作成フロー.md` 冒頭『最新の確定』・[[choukai-audio-pipeline]]。
- **🎨 UIバッチ（2026-07-23・未commit・tsc緑・test 9/9）**: ①ホーム犬1.3倍（`src/home/HomeCoach.tsx` compW×1.3）②試験カード「費用」→「受験費用」（`src/home/ExamInfoCard.tsx`）③設定JFTボタン非表示=準備中（`src/screens/ProfileScreen.tsx` mapで `ex==='jft'`→`return null`・分岐JLPT固定）④設定 試験日「未定」チップ非表示（JLPT枝）⑤**犬アニメ=ゆらゆら±3°＋数秒毎ぴょこ跳ね**（`HomeCoach.tsx` dogSway/dogHop・Animated.View で Image を包む）⑥**N3ルビ誤り＝データの二重ルビを機械修正**（`content/problems/dokkai/joho_{N5,N4,N3}.json`）: 送り仮名重複270（白（しろ）い（しろい）→白（しろ）い）＋同語反復31（参加（参加（さんか））→参加（さんか））＋誤挿入1（送って（はこんで）→送って）＝**計302除去・残0**。正常な注記は温存（N4「がなく（どなたでも）」「あり（なにもいらない）」＝読みでなく言い換え）。**rebuild済＝`_manifest.json`指紋更新**（OTA用）・bundled不変・content系test 9/9。**保留(a)ルビが漢字頭に被り上部欠け**＝別issue（レイアウト・全画面共通`RubyText.tsx`）→**この修正が届いた版で再確認＋スクショ待ち**（推定base `lineHeight:baseFs`窮屈だが全画面影響で慎重に）。**未commit/未push**（push=OTA配信のため合図待ち）。ビルドも合図時のみ。
- **🛠 情報検索クラッシュ修正=commit ec45baa・Build 2547（2026-07-23）**: Build2546配信後、情報検索タップで黒画面「問題が発生しました」→固着。真因=InfoSearchFigure.renderTable が `tbl.columns.map` を呼ぶが生成データの `block.table` が `[ヘッダ行,...データ行]` の配列で columns=undefined→即クラッシュ（405問中391問）。[[verify-runtime-not-just-build]]の典型（ビルド緑・gate.py緑でも実行時死＝gate.py が block.table 内部形を未検証）。対処=(A)データ391表を `{columns:row[0], rows:row[1:]}` に変換（content＋バックアップ annotated 両方）(B)renderTable を両形正規化＋undefinedガードに（防御）。tsc=content経路0・test 283pass/1fail(既存pgネパール)。**Pages/OTA run 29963863735=success＝既存ユーザーへ修正版OTA再配信済**。**Build run 29963868594=2547/both 全緑（build-ios=success/TestFlight提出済・build-android=success）＝配信完了（2026-07-23）**。次: ユーザーが TestFlight/実機で情報検索の復旧を目視確認。
- **✅ 情報検索 本番405問=commit 6d471d5・Build 2546/both 全緑・配信済（2026-07-23）**: build-ios success(submit=true→TestFlight提出済)＋build-android success＋Pages OTA run 29952952808 success（既存ユーザーへ405問OTA配信）。以下は起動時メモ→ ①〜④カスケード完了→生き残り **N5 110・N4 140・N3 155＝405問**（不合格5問=b014/b067/b114/b119/b137 は削除・backfillなし）。`content/problems/dokkai/joho_{N5,N4,N3}.json` へ反映＋`content/_manifest.json` 再生成（bundled.generated.tsはファイル一覧不変で差分なし＝正常・静的importでf29/f30/f31同梱を確認）。root tsc=content経路0エラー・`node --test` 283 pass/1 fail（唯一の赤=passageTransNe＝pg ネパール訳0/200の**既存**欠落で無関係。**ユーザー確定: 翻訳は日本語確定後に最後にまとめて実施＝現状の赤は無視でよい**。joho は InfoSearchFigure 描画でネパール訳導線が無いためテストから joho subtype 除外済）。**Build起動**: `gh workflow run build-jlpt.yml -f platforms=both -f build_number=2546 -f submit=true`＝**run 29952982089（iOS TestFlight＋Android・dispatch）**＋**Pages OTA run 29952952808（push契機・既存ユーザーへ新405問をOTA配信）**。次: 両run緑を確認しE2報告（iOS TestFlight＋Android＋OTA）。**D2累計(②Gemini)≒¥269**（③④Opusは¥0）。詳細=`memory\joho-gen-inflight.md`・退避先=`バックアップ\joho_pilot_20260722\`。
- **🏗 全画面ルビ被り修正＋SwipeSheet下スワイプ離脱＋試験情報1段化ほか＝commit 8da48a3・push済・Build 2544/both 投入中（run 29901982862・2026-07-22）**: `src/components/RubyText.tsx`(ルビ行高≥fontSize×1.25で漢字頭の被り解消・カタカナ＋かな読みはルビ無し)＋`SwipeSheet.tsx`(本文最上部で下スワイプ離脱)＋`ExamInfoCard.tsx`(カッコ内/次回の目安/ネット申込削除・1段表示)＋`AICoachScreen.tsx`新規/設定アイコン右端固定/`MyWordsScreen`整理。tsc=触ったファイル0エラー(既存11件は無関係)。次: Actions緑を確認しE2報告(iOS TestFlight＋Android APK・Play配信は合図時のみ)。**掃除**: 偽ワークフロー`.github/workflows/build.yml`(存在しない`./app`参照の壊れた未追跡下書き)を削除＝実ビルドは`build-jlpt.yml`のみ→[[build-jlpt-yml-is-canonical]]。**設定変更**: `~/.claude/settings.json` autoCompactWindow 200000→400000(ユーザー要望・セッション内記憶を延ばす／その分 往復コストは増える)。**⚠️情報検索 本番410問=作業ファイル喪失で中断中→詳細と再開方針は`memory\joho-gen-inflight.md`**(本番未反映・アプリは6問のテスト試行のみ)。
- **✅ ホーム画面6改修＋タブ背景の開始カード＝Build 2543成功・配信済（v1.1.0(2543)・commit 198921d・run 29895938326＋Pages 29895938195・2026-07-22）**: iOS(build-ios success/TestFlight)＋Android(build-android success)＋Pages OTA(run 29895938195 success)すべて緑。ユーザー選択「作業ツリー全部」＝6改修に加え未コミットだったStartCard機能(タブ背景の桜タップで『はじめる/復習』カード=`TabScene.tsx`/`DictHomeScreen`/`StudyHomeScreen`/`WordsHubScreen`)＋仲間犬png12枚も同梱(計21ファイル・explicit add・build.yml/pdf/cloudflare不含)。検証=rebuild差分なし・契約テスト8緑・tsc既存11件のみ(App47/dictView/BrowseScreen/supabase edge=無関係・出荷9ファイルに新エラー0)。**実機でダーク可読性・アドバイス位置(リングと桜の間)・レベル表示・桜タップ開始カードを目視確認推奨。** 6改修の中身↓／実機でダーク可読性・アドバイス位置・レベル表示を要目視。①ダーク統一=`src/components/SwipeSheet.tsx`をuseColors化(地/つまみ/×)→桜/犬/リングの各スワイプカードがダーク追従(中のExamInfo/成長/継続カードは元々テーマ対応)＋`HomeCoach.tsx`のショップ一覧itemCard等もc.ink/c.bgSoft/c.blueLight化。②レベル選択ピル削除=`App.tsx`(上部共通バーからピル＋N5/N4/N3メニュー＋未使用localを撤去)→`HomeScreen.tsx`で到達度の左に現在レベル(N5/N4/N3 or JFT)を同大表示(`lblRow`)。③アカウント未登録画面から成長/継続カード撤去=`AccountScreen.tsx`(StatCards関数＋import＋使用箇所を削除)。④🤖アドバイスボタン=`HomeScreen.tsx`最上部右に配置(insets.top+6)→タップで**リングと桜の間**(絶対配置 top+ringW+20・`adviceWrap`/`adviceCard`)にAIコーチ助言を表示(既存`home.ai_*`キー流用=弱点分野優先・合格率・今日の一手)。※当初は桜と犬の間だったがユーザー指示でリング直下へ移設(HomeCoach内の助言コードは撤去)。⑤カード地色=選択中テーマ色(`THEME_TINT`水彩5種＋light/dark/autoはc.blue・白文字＋薄影)。⑥桜/犬を下へ=`HomeCoach`の`wrap.bottom`60→20。root tsc=触った5ファイル0エラー(既存App47のsubmodule欠落等は無関係)。→**Build 2543で投入済(上記)**。
- **🏗 ホーム桜ペット＋合格リング試験情報＋アカウント整理＝commit 4aefcc9・push済・Build 2539/both 投入済(run 29861113407・2026-07-22)**: 変更=`src/home/HomeCoach.tsx`(桜の左に柴犬・既定柴1無料・柴タップで所持仲間選択・桜タップで所持済み髪型/着せ替え/筆のみ)＋`src/data/shop.ts`(仲間 柴2-6/黒1-6・homeScaleでランク拡大)＋`assets/shop/companion/{shiba,kuro}1-6.png`(12枚)＋`src/store/state.ts`/`storage.ts`(既定所持pet_shiba1・backfill)＋`src/home/ExamInfoCard.tsx`新規(合格リングタップ最上部=桜＋試験日/残日数/申込期間/費用・国内目安＋公式確認注記)＋`src/screens/HomeScreen.tsx`(リングタップmodalにExamInfoCard)＋`src/screens/AccountScreen.tsx`(ログイン中=ログイン状態＋ログアウトのみ)＋`App.tsx`(持ち物アイコン削除)＋`src/screens/ProfileScreen.tsx`(アカウント削除を設定最下部へ)。root tsc=触ったファイル0エラー(既存11件はApp47/dictView/BrowseScreen/supabase edgeで無関係)。**このBuild 2539は下の2538を内包(4aefcc9は5ec9a52の上)＝2538は上書き済**。次: Actions緑を確認しE2報告(iOS+Android/TestFlight＋Pages OTA)。失敗時のみログ確認。
- **🏗 情報検索(読解)刷新＝コンテンツ確定・型クリーン・commit 5ec9a52・Build 2539に内包済・2026-07-22**: 図表を4種ブロック(route/card/notice/table)へ刷新。`src/quiz/passageSet.ts`(Figure/FigureBlock型・readingToSetでfigure載せ)＋`src/components/InfoSearchFigure.tsx`(縦カード・route箱→↓→箱・rubyGate・即採点)＋`PassageSetPlayer.tsx`(figureルーティング)＝全て**root(CI実体)**。中N4トリム10件適用済(`naiyou_chu_N4.json` 25セ/100問)。joho各級2問=6問を`scratchpad\joho_gen_v2.mjs`(gemini-3-flash-preview)で再生成→**QA(`joho_qa.py`)で機械検査済**: 個人名0(「みなさん」は呼びかけでOK)・ルビ熟語単位/読みのみ(「参加（参加）」→「参加（さんか）」2件・「（選択肢外）」内部メモ1件を`joho_fix.py`で除去)・一意=1(経路図は時間+料金の2条件のみ=N4-2の曖昧さ解消)・字数目標超(360/392/565/426/1013/817)。**共通ルール強化済**=`md\00_共通情報.md`第V部6/7・第I部(個人名全面禁止=役割名のみ・ルビ熟語単位で読みのみ)。**情報検索作問手順を`md\09_読解.md`に記録**(図版3型/データ模型/字数/落とし穴)。**app/ ゴースト削除済**(node_modulesはrootへmv・以後`./node_modules/.bin/tsx`)。rebuild済・**root tsc=情報検索経路0エラー**(既存11件はApp/dictView/BrowseScreen/supabase edgeで無関係)。**commit 5ec9a52＋push済→ビルド投入済=Build 2538/both・run 29842821844(in_progress・2026-07-22)**。次: TestFlight提出/Actions結果を確認して緑ならE2報告(iOS+Android+OTA)。失敗時のみログ確認。Gemini実費 累計≒¥6(<¥1000)。QA偽陽性の学び=[[qa-name-ruby-false-positives]]。
- **✅ 読解「内容理解」品質シーケンス ①→④ 全完了・本番配信済（2026-07-20・API¥0・Opus）**: ①短の弱誤答差替(6問:N5 3/N4 2/N3 1)②中N5の弱誤答差替(7問・誤答8件／当初「作り直し」は精読で撤回・本文＋設問は公式もんだい5に沿う)③中N4に指示語同定10問を厳選新規作問(在庫中N4 100→110)④内容理解7ファイル(短/中/長×各級・情報検索除く)のトップレベルに`audit`(status=監査済み)タグ追記。全て**本文・正解・answerIndex不変**・choices訳i18n無しで翻訳無傷。apply=`scratchpad\apply_weak_swap.py`/`apply_shiji_add.py`/`add_audit_tag.py`(各`.bak_before_*`削除)→rebuild.ts(52)→テスト34/34緑→コミット→push→ビルド2528(v1.1.0)→iOS/Android/OTA全配信**成功**。記録訂正済=`md\09_読解.md`＋`memory\dokkai-quality-audit.md`(中N5「作り直し」→「誤答差替で対応済」)＋MEMORY索引。**⏸一意性(本文なしで解ける/2択化)の独立監査は保留(¥0枠・後で必要なら実施)。**
- **✅ 文法①保留4件の手動訂正＋言い換えsy:n3-v-1670採用 完了（未ビルド・2026-07-20）**: kb-003380(N5)/kb-003826・kb-003850・kb-003994(N3)の選択肢差し替え＋`hold`除去（kb-003826は問題文＋正解も差し替え）。言い換えは「将来のためになる」を**ひらがな「なる」に下線**で採用（verified=true・synonym_N3で1000/1000）。**重要=文法①の`hold`はアプリのどこもフィルタしていない＝保留4件は今まで第2正解つきで出題されていた**（grammarClozeOk.jsonはgrammar_formに無関係な死経路・実出題は`bankOf`=KNOWLEDGE_BANK由来）。rebuild＋契約テスト22/22＋在庫更新済（8345問・未検証1507）。修正スクリプト=`scratchpad\fix_holds_and_synonym.py`。**次ビルドに同乗**（RubyText行間・組み立て238と一緒）。
- **⏳ 文脈規定の未検証語を公式型で新規作問＝Geminiで作問・本番費用約2,000円の承認待ち（2026-07-20）**: 対象=N4 189＋N3 1318＝**計1507語**（N5残17は難所で別扱い・任意）。ルール=誤答5基本/下限3、第2の正解が出たら**誤答だけ削除(問題は捨てない)**・3未満は不成立で報告。データ=`app/content/problems/moji_goi/context_{LV}.json`（1問=prompt空所文・answer正解語・choices誤答プールのみ・表示時シャッフル）。**Geminiスクリプト=`scratchpad\context_gen\run.mjs`**（P1生成→P2答え隠し反証→不合格はP3作り直し1回。解説なし大問ゆえ検査段省略。gemini-3-flash-preview／バッチ8／`b*_final.json`キャッシュ再開）。**3語試作=布団(N4)/従う・刺激(N3)すべて成立・実費3.7円**（毛布を第2正解で自動除外を実証・1.22円/語）。**全1507語≒2,000円**（実測1.2〜1.3円/語。別件r605実測1.30円/語と一致）。本番コマンド=`node scratchpad/context_gen/run.mjs --level N4|N3 --run <name> --batch 8`。採用は別途 build 用スクリプトが要る（未作成）。参照=`md\03_文脈規定.md`。
  - **ユーザー選択=「N4先行→確認→N3」**。**✅ N4 189語 完了（BG `b84upef2a` exit0・2026-07-20）＝188採用可/1不成立・実費¥184**。不成立だった`cx:n4-v-195 客`は**✅手動修正済（2026-07-20）**＝レストラン注文枠に作り替え「客」一意（誤答5:店員/コック/いす/メニュー/荷物・店員は配膳側と文中明示）。`scratchpad\fix_n4_kyaku.py`で`_all.json`更新・status=OK・N4は**189/189成立**・API¥0。bak=`_all.json.bak_before_kyaku`。注意=採用判定は`ok`(184)でなく`status=='OK'`(188)で見る（P3作り直し分は`ok`未更新）。出力=`scratchpad\context_gen\out\gemini-3-flash-preview_n4\`。**✅ N3 完了（BG `bsmnibhtv` exit0・2026-07-20）＝総1278語/成立1273/不成立5・実費¥1,067**（gemini-3-flash-preview）。不成立5語=`預かる`/`偶`/`黙る`/`交ざる`/`混じる`＝後で手直しor除外。出力=`scratchpad\context_gen\out\gemini-3-flash-preview_n3\`（`_all.json`・`_usage.json`）。採用判定は`status=='OK'`で見る。文脈規定 作問 合計 N4¥184＋N3¥1,067＝**¥1,251**。**⏳ 採用build用スクリプトは未作成（context_{LV}.jsonへ書き戻す口が要る）＝N3/N4とも採用は別作業**。N4は客修正で189/189成立。
- **N5聴解の実測→`md\10_聴解.md`へN5追記 完了（2026-07-20・API課金¥0）**: 参考CD音声33本をローカル文字起こし(faster-whisper small int8＋pykakasiで拍数化)で実測し、10_聴解.mdに①総論のN5段落＋②実測サマリのN5表(4大問)を既存N3・N4と同フォーマットで追記済み。トラック対応＝開始01／課題理解:説明02例03設問04-10／ポイント理解:説明11例12設問13-18／発話表現:説明19例20設問21-25／即時応答:説明26例27設問28-33（計33で一致）。**実測値**: 本編(①②)平均3.45拍/秒＝**3級中もっとも遅い**(N4 3.55・N3 3.77)。①課題3.36≈②ポイント3.55(N3/N4と違い課題>ポイントにならない・±0.4内)。③発話2.37・④即時2.33拍/秒。スクリプト＝`scratchpad\n5_choukai_measure.py`／結果＝`scratchpad\n5_choukai_result.json`／ログ＝`scratchpad\n5_choukai_run.log`（温存）。
- **N5参考CDの吸い出し 完了（2026-07-20・Track34のみ読取不良でスキップ）**: `問題作成の参考\N5` に `Track01.mp3〜Track33.mp3`（33本・計約16.3MB・q2 VBR）を ffmpeg(libcdio) でリッピング済み。**Track34（最外周・約9秒）はドライブが物理読取でハング**→切り分けでシーク正常・T33まで正常・T34領域限定の不良と確認。ユーザーが「Track34不要」と判断→空ファイル削除・ffmpeg停止済み。元の`.cda`はフォルダに温存。再取得が要れば外周清掃か別ドライブ。API課金なし。
- **⏳ 試験タブ選択肢の行間バグ修正 済（未ビルド・2026-07-20）**: 折り返した2行目がカードからはみ出す原因＝`s.choiceTxt`の`flex:1`が`RubyText`の1文字ごとの基底テキストに乗り列の高さ計算が壊れていた。`app\src\components\RubyText.tsx`で基底テキストから`flex`系を除去（共有部品なので試験/単語/辞書の折り返し全部に効く。`flex`無し呼び出し側は無影響）。tsc緑。**次ビルド（下の605採用ビルド）に同乗させる**。実機で2段選択肢がカード内に収まるか要確認。
- **ビルドは1コマンド（2026-07-19 作成）**: `tools\build.ps1 -Message "..."` … manifest再生成→テスト+tsc→commit→push→Build番号算出→dispatch→監視まで一括。`-DryRun`で検証だけ。**最後に出したのは v1.1.0(2525)**（次は 2000+commit数 で自動算出される）。公開リリース(`publish`)の口はスクリプトに無い＝要る時だけ手で `gh workflow run`。
- **Build 2526＝成功・完了（2026-07-20）**: v1.1.0(2526)＝synonym245採用。run `29709986317` conclusion=success（iOS TestFlight／Android内部テスト＋Pages配信(OTA)すべて成功）。見張りBG `b87sfqnsa` はGitHub一時障害でスクリプト側がexit1になっただけでビルド本体は成功。二重起動分 `29709933574` の failure は想定内で無視。**注意: RubyText行間修正・下の組み立て238採用は2526に未同乗＝次ビルドに乗せる。**
- **文の組み立て＝除外365問の作り直し→アプリ採用まで完了（2026-07-20・API課金¥0）。次ビルドで配信。**
  - **採用（2026-07-20）**: ok=238問（N5 40/N4 100/N3 98）を `app\content\problems\bunpou\order_{N5,N4,N3}.json` の同id ambiguous項目へ上書き＋ambiguousフラグ解除＝在庫化。残ambiguous127（要なおし86＋語順自由な副詞41）は除外維持。**在庫 組み立て 420→658**（N5 96→136・N4 114→214・N3 210→308）／監査落ち365→127。全238件が不変条件（answer∈choices・部品4・★2番目・空欄4）通過・スキップ0。契約テスト緑（bankId動的件数／saveRef／splitKB ambiguous維持）・tsc0・`rebuild.ts`でbarrel再生成・`stock_report.py`更新済み。pointId=null（文法id付けは別作業・cleanにもnullあり）。バックアップ＝`order_{lv}.json.bak_before_order_adopt`。スクリプト＝`scratchpad\order_n5\adopt.py`。
  - もとの除外(ambiguous)＝N5 65 / N4 146 / N3 154 = 365問（出題プールの46%が落ちていた）
  - 位置が動く副詞（どうして/まだ/さっき/まさか等）は**作問対象外**＝ユーザー判断。N5 11・N4 11・N3 19 を除外 → 作問324問
  - 結果 **採用できる238問**（N5 40 / N4 100 / N3 98）・要なおし86問。実費 **1,069円**（gemini-3-flash-preview・204リクエスト）
  - Excel＝`文の組み立て_作り直し.xlsx`（級ごとに採用/要なおしシート＋集計）
  - **効いた設計**: ①語順が固定される完成文を先に作り、後で文節に割る ②**答えを隠して**「自然に並ぶ形を全部挙げよ」と別リクエストで聞く（自己採点では非一意を見逃す）→ 落ちた86問中**76問がこの反証で検出**
  - スクリプト: `scratchpad\order_n5\run.mjs --level N5|N4|N3`（済みバッチは再利用＝二重課金しない）/ `review.py`（Excel）
- **✅ 言い換え類義N3＝残り605問の作り直し→アプリ採用まで完了（2026-07-20・ユーザー指示で採用）。次ビルドで配信。**
  - **採用（2026-07-20）**: `build_adopt.py --run r605 --apply` で **604語採用**（下線が機械特定できない1語 `sy:n3-v-1670` 為る/役に立つ＝ひらがな「なる」でfugashiが別語判定→スキップ・未検証のまま残す）。**verified 395→999**（`synonym_N3.json` N3 1000中／未検証1）。**第2の正解60語は誤答から除去（規則通り・除去後も誤答≥3を全件確保）**。採用後の実データ突き合わせ検証＝**第2正解が誤答に残る違反0件**（照合で出た「非drop19件」は fitWords が正解の別活用形だった私の照合の誤検出＝実害なし）。
  - **反映**: `rebuild.ts`（52ファイル再生成）→ 契約テスト `node --import tsx --test synonymFormat.test.ts daimon4choices.test.ts` **22/22緑**（※vitestではなくnode:testで回す）→ `stock_report.py`（在庫更新・言い換えN3 未検証605→1）。バックアップ=`synonym_N3.json.bak_before_adopt`。build_adopt.py に `--run <name>` 引数を追加（p200/r605切替）＋「下線不明はスキップ」化。
  - 実費: 生成は既走（b50mmlo23 exit0）＝採用フェーズで新規API課金なし。救済ルール＝[[second-answer-drop-distractor-not-question]]。make_excel は未実行（必要なら別途）。
  - **⏳ ビルド走行中 BG `bm6s08f3g`（2026-07-20 起動・`build.ps1 -Message "…604語採用・組み立て238採用・選択肢行間修正"`）**＝この3点を同乗（605採用＋組み立て238＋RubyText行間）。Build番号=2000+commit数（push後算出）。完了通知でrun結果とBuild番号を確認して報告。ログ=タスク出力 `bm6s08f3g.output`。
- **言い換え類義N3＝先発245問すべてアプリ採用済み（2026-07-20）。テスト緑・在庫更新済み。追加のアプリ実装は不要だった。**
  - **採用245の内訳**: クリーン**203**＋活用形（下線=活用表層）**20**＋救済（第2の正解を除いて誤答5で採用）**22**。verified 150→**395**（`synonym_N3.json` の N3 1000問中）。
  - **新ルール適用**: 第2の正解があった22問は問題ごと捨てず、その誤答だけ除いて採用（誤答>3の全作問共通ルール＝[[second-answer-drop-distractor-not-question]]）。除去後の誤答は21問が5個・1問（語る）が4個で、いずれも4択表示の最低3個を満たす。
  - **なぜ200希望→245採用**: 生成時、約18%の落ち率を見込んで「使える200」に届くよう245語作った。救済ルールで落ちる予定の22も生き返り、245全部が採用に。
  - **形式(b)＝アプリ改修不要と判明**: `daimon.ts` の `build4Choices` が誤答プール(N>3)から毎回3つ抽選して4択表示する既存ロジック。データに誤答6（救済は5/4）をプールで持たせるだけで成立。
  - **活用形(B)＝アプリ改修不要と判明**: `underlineSegments` は `sentence.indexOf(下線語)`。下線データに活用表層（争う→**争った**、気に入る→**気に入りました**等）を入れれば下線が引ける。生成時に正解・誤答も活用形へ揃え済みなので置き換えも成立。
  - **解説なし**: 採用データから `i18n`（explain）を除去済み。`rehydrate.ts` は `it.i18n?.ja?.explain` の任意参照なので無害。
  - **反映手順（実施済み）**: `synonym_N3.json` を in-place 更新（バックアップ `synonym_N3.json.bak_before_adopt` あり）→ `node --import tsx tools/content/rebuild.ts` で manifest+barrel 再生成（52ファイル）→ 契約テスト `synonymFormat.test.ts` **15/15**・`daimon4choices.test.ts` **7/7** 緑 → `tools/stock_report.py` 実行。
  - **確認用Excel＝`言い換え類義N3_確認.xlsx`**（セッション直下・最終版）。1シート「採用245」（クリーン→活用形（黄）→救済（青）の順・救済は除いた第2の正解を明記）＋集計。
  - **実費 合計 約313円**（gemini-3-flash-preview。今回の採用フェーズで新規API課金なし）。走行スクリプトは `scratchpad\synonym_n3\`（run.mjs / build_adopt.py / make_excel.py・正本 out\…\_all.json）に温存。
  - **残（任意）**: 問題なく動くなら `.bak_before_adopt` は次のビルド後に削除可。次ビルドで配信（OTA/ストア）に乗る。
- **読解 内容理解（短/中/長）＝公式意図との一致監査 完了（2026-07-20・実データ全357セット/649問・subagent3体・API実費¥0）**。
  - **結論: 出題意図・正解の狙い・誤答の焦点ずらしは全級で公式型に一致。作り直しが要るのは1か所＝中文N5のみ。ほかは軽い手当て。**
  - **🔴要修正=中文N5**: 指示語（下線）の設問が27セット全てに無く、25/27が細部照合ペアの寄せ集め＝公式の「理由＋指示内容」2層になっていない（正解の妥当性自体は一致）。→ 指示語問題の作問が要る（有料・見積もり要）。
  - **🟠データ破損（ローカル修正¥0）**: 短N5 `script017`主語Cさん本文不在／短N4 `script038`ルビ枠破損／長N3 set4・set8 ルビ破損／短N4 `tan-1`合算問題の一意性弱い。
  - **🟡型/字数の軽微ズレ**: 短N3に下線「それ」指示語型が無い／長N3の主張問題は「一般論の罠」が弱く消去法耐性やや低い／字数は多くが公式レンジより短め（中N4は約25%短・中N3はやや長）。
  - answerIndex全件0は無害（`app\src\components\PassageSetPlayer.tsx`が描画時シャッフル）。
- **文章の文法＝新問題200セットをアプリへ採用済み（2026-07-19）**。旧問題は退避済み＝在庫に数えない。
  - 採用: N5 80 / N4 80 / N3 40 = **200セット・1000問**（`app\content\problems\bunpou\passage_grammar_{N5,N4,N3}.json`・全件マーカー数と4択を機械検証 異常0）
  - 旧問題の退避先: `バックアップ\文章の文法_旧_20260719\`（移動済み。戻さない限り在庫にも入らない）
  - 確認用Excel（セッション直下）: `文章の文法_N5.xlsx` / `文章の文法_N4.xlsx` / `文章の文法_N3.xlsx`
  - 正解位置はシャッフル済み（1:217 / 2:265 / 3:246 / 4:272）。id固定シードなので再実行しても同じ並び。
- **ルビ付け＝完了（2026-07-19）**。方式と実績は `md\08_文章の文法.md` の「ルビ付け」章に記録済み（毎回は読まない）。
  - 結果: **付与率 N5/N4/N3 とも100%**（585/3,024/2,969箇所）・**本文のずれ0件**・QC 0件。計15体・46.3万トークン。
  - **正本＝`scratchpad\pg\ruby_applied.json`**。`finalize.py`（Excel再出力）も `adopt.py`（アプリ採用）もこれを見るよう切替済み＝再実行しても二重にルビが付かない／シャッフルし直さない／旧問題の控えを潰さない。
- **アプリ側の形も揃えた（テストが契約）**。詳細は `md\08_文章の文法.md`「アプリ側が要求する形」章。
  - N3の本文を3ブロック→**1ブロック**に連結（テスト要求。改行は残すので表示は不変）／設問idを `pg-<級>-<3桁>-q<n>` に振り直し。
  - `passageGrammar.test.ts` / `passageGrammarWire.test.ts` のセット数を 120→**200**（N5 80/N4 80/N3 40）に更新。
- **pointId＝完了・出荷済み（2026-07-19）**。1000問すべてに文法項目IDを付与し、`passageGrammar.test.ts` は green。
  - **v1.1.0(2525) 成功・TestFlight提出済/Android内部テスト**（run 29681310740）。OTA配信(Pages)も成功（run 29681306795）。
  - 実績: 2周・計7体・**48.4万トークン**（見積もり10万＝**約5倍の過小見積もり**。1問ごとの判断でツール往復が増える分を読み違えた）。
  - **失敗と対策（再発防止）**: 1周目は各級に**同じ級の候補しか渡さず**、N3が200問→30種類に集中・low 86%。文章の文法の空欄は**下の級の接続詞・助詞を文脈で問う**ので、候補は必ず**級以下の全項目**にする（テストの `gids` も級を問わず全393を許容）。振り直し後 N3=46種類・low 29。
  - 残る low: **N5 137件**（grammar.json に助詞・時制の項目が無いため救済不能。下に級も無い）。
  - スクリプト（scratchpad・LLM不要）: `pointid_prep.js`（材料）/ `pointid_probe.js`（機械照合の当たり率＝283/1000）/ `pointid_retry_prep.js`（low だけ再抽出）/ `pointid_apply.js`（検証＋書き戻し・retry優先）。
- **残っている未了（LLMが要る）**
  1. **翻訳(i18n)** … ユーザー指示で保留。今は languages:['ja'] のみ。`app\src\data\exam\passageTransNe.test.ts` が全200セットのネパール語訳を要求するので、**保留中はこのテストも落ちたまま**（想定内。CIにテスト段は無いのでビルドは通る）。
  2. **本文の加長** … 字数が目標未達（中央値 N5 190/250・N4 207/340・N3 354/425）。N4の不足が大きい。
- **作業スクリプト（全部ローカル・LLM不要）**: `scratchpad\pg\` に rescue.py（中断救済）/ make_verify_rest.py（残りだけ再生成）/ assemble_qc.py（統合＋品質集計）/ finalize.py（シャッフル＋Excel）/ adopt.py（退避＋採用）。材料と最終形も同フォルダ（final_all.json＝正本）。
- **中断したら救済（run IDを覚えていなくてよい）**: `python scratchpad\pg\rescue.py --since 2026-07-18` → 残りが missing_ids.txt に出る → `python scratchpad\pg\make_verify_rest.py --only scratchpad\pg\missing_ids.txt` で残りだけ作り直して Workflow 起動。残ゼロまで繰り返す（id後勝ちで二重にならない）。


## 文脈の床（基準値・2026-07-19 実測）
- `/clear` 直後の床 = **40.1k / 200k（20%）**。増加は約 760→514→400 トークン/往復と逓減（往復21:46,424 / 24:48,708 / 30:51,791 / 33:52,992）。
- 注意: 最小化前の床は記録が無く、削減幅は不明。以後は変更のたびにここを上書きする。

<!-- AUTO:BEGIN -->

## 走行中の run（自動・完了通知が来ていないもの）
- a24a55339e7688334 general-purpose
- a299080a95b15e8d3 general-purpose

## 直近24時間の変更ファイル（自動）
- docs/superpowers/specs/2026-07-26-monetization-design.md
- memory/session-summary-LATEST.md
- memory/handoff.md
- content/_manifest.json
- src/data/content/bundled.generated.ts
- 問題/聴解/聴解_TTSスクリプト_N4.xlsx
- 問題/聴解/聴解_TTSスクリプト_N3.xlsx
- memory/choukai_gen_log.txt

_自動更新: 2026-07-26 08:30_
<!-- AUTO:END -->
